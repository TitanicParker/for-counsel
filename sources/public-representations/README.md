# Public representations

**Status:** CONTROLLING — transitional path map

The current public redacted source representations remain in `docs/source-records/` so existing links do not break. They are not duplicated here. Migration requires a path map and successful link validation before any move.
