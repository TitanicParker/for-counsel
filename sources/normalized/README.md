# Normalized source text

**Status:** CONTROLLING — derived layer

Normalized text is derived search/readability material. Every file must identify its source ID and must have an entry in `change-log.csv`. A missing change-log entry is a validation failure.
