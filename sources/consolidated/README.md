# Consolidated corpora

**Status:** CONTROLLING — derived layer

Each corpus must state its inputs and order; whether each input is native text, OCR or transcription; every normalization applied; generation date; generating commit; source hashes; and that the original/best available source remains controlling.

The clinical pilot is deliberately small. It validates the identifier and provenance system before bulk assignment across the year-split notes.
